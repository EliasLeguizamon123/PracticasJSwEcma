const producto1 = 'Monitor de 20';

console.log(producto1);

//Conocer el nro de caracteres en el string
console.log(producto1.length);

// Buscar posicion de una palabra o caracter dentro de un string
console.log(producto1.indexOf('Monitor'));

//Buscar si existe dentro del string
console.log(producto1.includes('Monitor'));